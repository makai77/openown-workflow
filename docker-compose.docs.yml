services:
  docs:
    image: openown_local_docs
    container_name: openown_local_docs
    build:
      context: .
      dockerfile: ./compose/local/docs/Dockerfile
    env_file:
      - ./.envs/.local/.django
    volumes:
      - /app/.venv
      - ./docs:/docs:z
      - ./config:/app/config:z
      - ./openown:/app/openown:z
    ports:
      - '9000:9000'
    command: /start-docs
