// Placeholder shell. Screens, routing and the auth provider land in later slices;
// this slice ships only the api/ layer and the shared UI primitives.
function App() {
  return (
    <main className="grid min-h-svh place-items-center p-8 text-center">
      <div>
        <h1 className="text-2xl font-semibold">Open Ownership Workflow</h1>
        <p className="text-gray-500">Frontend scaffold ready.</p>
      </div>
    </main>
  );
}

export default App;
