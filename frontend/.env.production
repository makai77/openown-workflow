# Production build config (Vite inlines this at build time, not runtime).
# Relative base: the SPA calls the API same-origin through the Apache reverse
# proxy (https://<host>/api -> Gunicorn), so no domain is hardcoded in the bundle.
VITE_API_BASE_URL=/api
